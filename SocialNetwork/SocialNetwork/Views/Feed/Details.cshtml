@model SocialNetwork.Models.Feed

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Details</h1>

<div>
    <h4>Feed</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.FeedId)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.FeedId)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { id = Model.FeedId }) |
    <a asp-action="Index">Back to List</a>
</div>
<div>
    <h3>Posts on feed</h3>
    @{
        int i = 1;
        foreach (var post in Model.Posts)
        {
    <div>@post @Html.ActionLink("View", "Details", "Post", new { id = post })</div> 
        }
    }
  
</div>
